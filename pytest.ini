[tool:pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --tb=short -v
# Temporarily commented out to see progress: --cov-fail-under=80
filterwarnings =
    ignore::DeprecationWarning